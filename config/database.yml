# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000  
  schema_search_path: "public, pim, crawler, app, crawled_data"

development:
  <<: *default
  host: localhost
  database: kinator_production
  username: postgres
  password: paxcom123  

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
dev:
  <<: *default
  host: 172.16.0.61
  database: kinator
  username: postgres
  password: paxceldevPIMQWERRTY

   
